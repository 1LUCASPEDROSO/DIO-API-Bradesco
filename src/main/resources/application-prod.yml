spring:
  springdoc:
    api-docs:
      version: 3.0.1
  datasource:
    url: jdbc:postgresql://${URL}
    username: ${PGUSER}
    password: ${PGPASSWORD}
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
      dialect: org.hibernate.dialect.PostgreSQLDialect